CREATE OR REPLACE TABLE "DIMENSION_PRODUCT" AS 

WITH CTE AS (
SELECT DISTINCT "product"
FROM "ZALANDO_ALL"
UNION ALL
SELECT DISTINCT "product"
FROM "VINTED_ALL"
UNION ALL
SELECT DISTINCT "product"
FROM "BAZOS_ALL"
UNION ALL
SELECT DISTINCT "product"
FROM "ZOOT_ALL")
SELECT DISTINCT *
FROM CTE
;
